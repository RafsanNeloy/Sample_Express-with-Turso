{
     "name": "Go Web Server Development",
     "image": "golang:1.22",
     "forwardPorts": [
       8080
     ],
     "postCreateCommand": "apt-get update && apt-get install -y sudo && go mod tidy"
   }
